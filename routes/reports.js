const express = require('express');\nconst router = express.Router();\nconst pdfService = require('../services/pdfService');\nconst measurementService = require('../services/measurementService');\n\n// Generate daily report PDF\nrouter.get('/daily/:date', async (req, res) => {\n  try {\n    const { date } = req.params;\n\n    // Validate date format (YYYY-MM-DD)\n    if (!/^\\d{4}-\\d{2}-\\d{2}$/.test(date)) {\n      return res.status(400).json({ error: 'Invalid date format. Use YYYY-MM-DD' });\n    }\n\n    // Generate PDF\n    const filePath = await pdfService.generateDailyReport(date);\n\n    // Send file\n    res.download(filePath, `Daily_Report_${date}.pdf`);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Get daily statistics\nrouter.get('/stats/:date', async (req, res) => {\n  try {\n    const { date } = req.params;\n    const stats = await measurementService.getMeasurementStats(date);\n    res.json(stats);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Get part statistics\nrouter.get('/stats/part/:partId/:date', async (req, res) => {\n  try {\n    const { partId, date } = req.params;\n    const stats = await measurementService.getMeasurementStatsByPart(partId, date);\n    res.json(stats || { ok_count: 0, ng_count: 0, retake_count: 0, total: 0 });\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nmodule.exports = router;\n