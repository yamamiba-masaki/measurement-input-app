const express = require('express');\nconst router = express.Router();\nconst measurementService = require('../services/measurementService');\n\n// Get all master data\nrouter.get('/', async (req, res) => {\n  try {\n    const masterData = await measurementService.getAllMasterData();\n    res.json(masterData);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Get master data by ID\nrouter.get('/:id', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const masterData = await measurementService.getMasterDataById(id);\n    if (!masterData) {\n      return res.status(404).json({ error: 'Master data not found' });\n    }\n    res.json(masterData);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\n// Get measurement locations for a part\nrouter.get('/:id/locations', async (req, res) => {\n  try {\n    const { id } = req.params;\n    const locations = await measurementService.getLocationsByPartId(id);\n    res.json(locations);\n  } catch (error) {\n    res.status(500).json({ error: error.message });\n  }\n});\n\nmodule.exports = router;\n